package com.example.fxraptor.repository;

import com.example.fxraptor.domain.MarginRule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MarginRuleRepository extends JpaRepository<MarginRule, String> {
}
