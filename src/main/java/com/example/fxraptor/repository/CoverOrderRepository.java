package com.example.fxraptor.repository;

import com.example.fxraptor.domain.CoverOrder;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CoverOrderRepository extends JpaRepository<CoverOrder, Long> {
}
